{
  "name": "netlify-mcp-server",
  "version": "1.0.0",
  "description": "Netlify MCP Server with improved origin checking",
  "main": "netlify/functions/mcp.ts",
  "scripts": {
    "build": "tsc",
    "deploy": "netlify deploy --prod",
    "dev": "netlify dev"
  },
  "dependencies": {
    "@netlify/functions": "^2.0.0",
    "minimatch": "^10.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
